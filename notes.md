GeoVAS - Notes de mise a jour

- Cache runtime des tuiles OpenStreetMap pour usage hors-ligne (zones deja consultees).
- Ajout d'une file d'attente locale pour les caches ajoutes hors-ligne et sync auto au retour reseau.
- Sync manuel et auto toutes les 10 s conserve, avec envoi des caches en attente avant chaque sync.
